{
  "name": "awb-web",
  "version": "2.0.0",
  "description": "SiCepat AWB Tracker - Web Edition (Vercel)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev --listen 3000",
    "build": "echo 'Static + serverless, no build step needed'"
  }
}
